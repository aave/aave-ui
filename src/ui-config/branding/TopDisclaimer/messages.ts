import { defineMessages } from 'react-intl';

export default defineMessages({
  disclaimer:
    'Always make sure the URL is {link} - bookmark it to be safe. Aave will never ask for your private keys or seed phrase. Do not give out this information to anyone or any site.',
});
