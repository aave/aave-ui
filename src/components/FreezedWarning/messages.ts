import { defineMessages } from 'react-intl';

export default defineMessages({
  migrationText:
    'Supply and borrow of {token} is disabled due to the ongoing migration. You can still repay and withdraw to migrate your token {here}',
  here: 'here',
  freezedText: 'Supply and borrow of {token} is disabled',
});
