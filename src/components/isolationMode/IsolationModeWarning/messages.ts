import { defineMessages } from 'react-intl';

export default defineMessages({
  caption: 'Isolation mode alert',
  description:
    'This asset can be supplied as collateral in isolation mode only. Learn more in {link}.',
  faqGuide: 'FAQ guide',
});
