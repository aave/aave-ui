import { defineMessages } from 'react-intl';

export default defineMessages({
  caption: 'Select the reward you want to claim or claim all rewards at once',
  claimAll: 'Claim all',
  claim: 'Claim {symbol}',
});
